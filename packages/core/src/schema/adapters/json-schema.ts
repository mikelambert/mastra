// Re-export everything from @mastra/schema-compat for backwards compatibility
export {
  JsonSchemaWrapper,
  toStandardSchema,
  type JsonSchemaAdapterOptions,
} from '@mastra/schema-compat/adapters/json-schema';
